import { Demo } from './demo'
import { Demo as DemoSplit } from './demo-split'
import { Demo as DemoStagger } from './demo-stagger'

<DemoCanvas>
  <DemoPreview>
    <Demo />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo.vue
    ```
  </DemoCode>
</DemoCanvas>

## Installation

<InstallationTabs item="slide-up-text">
```vue file=../../vue/src/components/ui/SlideUpText.vue
```
</InstallationTabs>

## Examples

### Split By

<DemoCanvas>
  <DemoPreview>
    <DemoSplit />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo-split.vue
    ```
  </DemoCode>
</DemoCanvas>

### From

<DemoCanvas>
  <DemoPreview>
    <DemoStagger />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo-stagger.vue
    ```
  </DemoCode>
</DemoCanvas>

## Usage

```vue
import { SlideUpText } from "@/components/ui";
```
```vue
<SlideUpText>Your text here</SlideUpText>
```

## Props

<PropsTable
    data={[
      {
        name: "default",
        type: "VNodeChild",
        nameDetails: "The text content to animate"
      },
      {
        name: "split",
        type: "'words' | 'characters' | 'lines'",
        default: "words",
        typeDetails: "How to split the text for animation"
      },
      {
        name: "delay",
        type: "number",
        default: "0",
        nameDetails: "Delay before the first element starts animating (in seconds)"
      },
      {
        name: "stagger",
        type: "number",
        default: "0.1",
        nameDetails: "Delay between each element animation (in seconds)"
      },
      {
        name: "from",
        type: "'first' | 'last' | 'center'",
        default: "first",
        typeDetails: "Where to start the stagger animation from"
      },
      {
        name: "transition",
        type: "AnimationOptions",
        default: "{ type: 'tween', ease: [0.625, 0.05, 0, 1], duration: 0.5 }",
        nameDetails: "Motion transition configuration"
      },
      {
        name: "autoStart",
        type: "boolean",
        default: "true",
        nameDetails: "Whether to start animation automatically on mount"
      },
      {
        name: "class",
        type: "string",
        nameDetails: "Additional CSS classes for the container"
      },
      {
        name: "wordClass",
        type: "string",
        nameDetails: "Additional CSS classes for word wrapper"
      },
      {
        name: "charClass",
        type: "string",
        nameDetails: "Additional CSS classes for character wrapper"
      },
      {
        name: "onStart",
        type: "() => void",
        nameDetails: "Callback when animation starts"
      },
      {
        name: "onComplete",
        type: "() => void",
        nameDetails: "Callback when animation completes"
      },
      {
        name: "in-view",
        type: "boolean",
        default: "false",
        nameDetails: "Whether to trigger animation only when element is in viewport"
      }
    ]}
  />
