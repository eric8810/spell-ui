import { Demo } from './demo'
import { Demo as DemoCount } from './demo-count'

<DemoCanvas>
  <DemoPreview>
    <Demo />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo.vue
    ```
  </DemoCode>
</DemoCanvas>

## Installation

<InstallationTabs item="logos-carousel">
```vue file=../../vue/src/components/ui/LogosCarousel.vue
```
</InstallationTabs>

## Examples

### Logo Count

Limit the number of logos displayed per row.

<DemoCanvas>
  <DemoPreview>
    <DemoCount />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo-count.vue
    ```
  </DemoCode>
</DemoCanvas>

## Usage

```vue
import { LogosCarousel } from "@/components/ui";
```

```vue
<LogosCarousel :count='4'>
  <YourLogo1 />
  <YourLogo2 />
  <YourLogo3 />
  <YourLogo4 />
  <YourLogo5 />
  <YourLogo6 />
</LogosCarousel>
```

## Props

<PropsTable
  data={[
    {
      name: "default",
      type: "VNodeChild",
      nameDetails: "Logo elements to display in the carousel. The carousel will automatically group them based on the count prop."
    },
    {
      name: "stagger",
      type: "number",
      default: "0.14",
      nameDetails: "Delay in seconds between each logo animation"
    },
    {
      name: "count",
      type: "number",
      nameDetails: "Number of logos to display per row. The carousel will automatically group children into sets of this size. If not specified, all logos will be displayed in a single group."
    },
    {
      name: "class",
      type: "string",
      nameDetails: "Additional CSS classes to apply to the flex container. Use Tailwind gap classes (e.g., gap-4, gap-8) to control spacing between logos."
    },
    {
      name: "duration",
      type: "number",
      default: "600",
      nameDetails: "Animation duration in milliseconds"
    },
    {
      name: "interval",
      type: "number",
      default: "2500",
      nameDetails: "Time in milliseconds between carousel transitions"
    },
    {
      name: "initialDelay",
      type: "number",
      default: "500",
      nameDetails: "Initial delay in milliseconds before the animation starts"
    }
  ]}
/>
