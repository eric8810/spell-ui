import { Demo } from './demo'
import { Demo as DemoVariant } from './demo-variant'
import { Demo as DemoSizes } from './demo-sizes'

<DemoCanvas>
  <DemoPreview>
    <Demo />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo.vue
    ```
  </DemoCode>
</DemoCanvas>

## Installation

<InstallationTabs item="pop-button">
```vue file=../../vue/src/components/ui/PopButton.vue
```
</InstallationTabs>

<details>
<summary>Font Setup (Optional)</summary>

To use the playful DynaPuff font with this component:

1. Add the font to your `src/style.css`:

```css
@import url("https://fonts.googleapis.com/css2?family=DynaPuff:wght@400..700&display=swap");

:root {
  --font-pop: "DynaPuff", cursive;
}
```

</details>

## Examples

### Color

<DemoCanvas>
  <DemoPreview>
    <DemoVariant />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo-variant.vue
    ```
  </DemoCode>
</DemoCanvas>

### Size

<DemoCanvas>
  <DemoPreview>
    <DemoSizes />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo-sizes.vue
    ```
  </DemoCode>
</DemoCanvas>


## Usage

```vue
import { PopButton } from "@/components/ui";
```

```vue
<PopButton color="violet" size="default">
  Button
</PopButton>
```

## Props

<PropsTable
  data={[
    {
      name: "default",
      type: "VNodeChild",
      required: true,
      nameDetails: "The content to display inside the button"
    },
    {
      name: "color",
      type: "'default' | 'blue' | 'purple' | 'pink' | 'red' | 'orange' | 'yellow' | 'green' | 'teal' | 'cyan' | 'indigo' | 'violet' | 'rose' | 'amber' | 'lime' | 'sky' | 'slate' | 'gray' | 'zinc' | 'neutral' | 'stone' | 'fuchsia' | 'emerald'",
      default: "'default'",
      nameDetails: "The color of the button - supports all Tailwind colors"
    },
    {
      name: "size",
      type: "'sm' | 'default' | 'lg'",
      default: "'default'",
      nameDetails: "The size of the button"
    },
    {
      name: "disabled",
      type: "boolean",
      default: "false",
      nameDetails: "Whether the button is disabled"
    },
    {
      name: "@click",
      type: "() => void",
      nameDetails: "Click event handler"
    },
    {
      name: "class",
      type: "string",
      nameDetails: "Additional CSS classes to apply"
    },
    {
      name: "asChild",
      type: "boolean",
      default: "false",
      nameDetails: "Change the component to the child component"
    }
  ]}
/>
