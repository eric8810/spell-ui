import { Demo } from './demo'
import { Demo as DemoDirection } from './demo-direction'

<DemoCanvas>
  <DemoPreview>
    <Demo />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo.vue
    ```
  </DemoCode>
</DemoCanvas>

## Installation

<InstallationTabs item="highlighted-text">
```vue file=../../vue/src/components/ui/HighlightedText.vue
```
</InstallationTabs>

## Examples

### Direction

<DemoCanvas>
  <DemoPreview>
    <DemoDirection />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo-direction.vue
    ```
  </DemoCode>
</DemoCanvas>

## Usage

```vue
import { HighlightedText } from "@/components/ui";
```

```vue
<HighlightedText>
  Your text here
</HighlightedText>
```

## Props

<PropsTable
  data={[
    {
      name: "default",
      type: "VNodeChild",
      required: true,
      nameDetails: "The text content to highlight"
    },
    {
      name: "from",
      type: "'left' | 'right' | 'top' | 'bottom'",
      default: "'bottom'",
      nameDetails: "Direction from which the background slides in"
    },
    {
      name: "delay",
      type: "number",
      default: "0",
      nameDetails: "Animation delay in seconds"
    },
    {
      name: "in-view",
      type: "boolean",
      default: "false",
      nameDetails: "Whether to trigger animation only when element is in viewport"
    },
    {
      name: "class",
      type: "string",
      nameDetails: "Additional CSS classes to apply"
    }
  ]}
/>