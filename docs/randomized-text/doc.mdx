import { Demo } from './demo'
import { Demo as DemoSplit } from './demo-split'

<DemoCanvas>
  <DemoPreview>
    <Demo />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo.vue
    ```
  </DemoCode>
</DemoCanvas>

## Installation

<InstallationTabs item="randomized-text">
```vue file=../../vue/src/components/ui/RandomizedText.vue
```
</InstallationTabs>

## Examples

### Split By

<DemoCanvas>
  <DemoPreview>
    <DemoSplit />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo-split.vue
    ```
  </DemoCode>
</DemoCanvas>

## Usage

```vue
import { RandomizedText } from "@/components/ui";
```

```vue
<RandomizedText>You can just ship things.</RandomizedText>
```

## Props

<PropsTable
  data={[
    {
      name: "default",
      type: "string",
      required: true,
      nameDetails: "The text to display with randomized animation"
    },
    {
      name: "class",
      type: "string",
      nameDetails: "Additional CSS classes for the container"
    },
    {
      name: "split",
      type: "'words' | 'chars'",
      default: "'words'",
      nameDetails: "How to split the text for animation - by words or characters"
    },
    {
      name: "delay",
      type: "number",
      default: "0.2",
      nameDetails: "Base delay before animation starts"
    },
    {
      name: "in-view",
      type: "boolean",
      default: "false",
      nameDetails: "Whether to trigger animation only when element is in viewport"
    }
  ]}
/>