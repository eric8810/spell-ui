import { Demo } from './demo'
import { Demo as DemoSpeed } from './demo-speed'

<DemoCanvas>
  <DemoPreview>
    <Demo />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo.vue
    ```
  </DemoCode>
</DemoCanvas>

## Installation

<InstallationTabs item="special-text">
```vue file=../../vue/src/components/ui/SpecialText.vue
```
</InstallationTabs>

## Examples

### Speed

<DemoCanvas>
  <DemoPreview>
    <DemoSpeed />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo-speed.vue
    ```
  </DemoCode>
</DemoCanvas>

## Usage

```vue
import { SpecialText } from "@/components/ui";
```
```vue
<SpecialText>SPELL UI</SpecialText>
```

## Props

<PropsTable
  data={[
    {
      name: "default",
      type: "string",
      nameDetails: "The text content to animate"
    },
    {
      name: "speed",
      type: "number",
      default: "20",
      nameDetails: "Animation speed in milliseconds (lower is faster)"
    },
    {
      name: "delay",
      type: "number",
      default: "0",
      nameDetails: "Delay before starting the animation (in seconds)"
    },
    {
      name: "class",
      type: "string",
      default: "\"\"",
      nameDetails: "Additional CSS classes for styling"
    },
    {
      name: "in-view",
      type: "boolean",
      default: "false",
      nameDetails: "Whether to trigger animation only when element is in viewport"
    }
  ]}
/>