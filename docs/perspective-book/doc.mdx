import { Demo } from './demo'
import { Demo as DemoTextured } from './demo-textured'

<DemoCanvas>
  <DemoPreview>
    <Demo />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo.vue
    ```
  </DemoCode>
</DemoCanvas>

## Installation

<InstallationTabs item="perspective-book">

```vue file=../../vue/src/components/ui/PerspectiveBook.vue
```

</InstallationTabs>

## Usage

```vue
import { PerspectiveBook } from "@/components/ui";
```
```vue
<PerspectiveBook>
  <div class="flex flex-col gap-4">
    <h1 class="font-semibold leading-5">
      Your complete platform for the Design.
    </h1>
    <BookOpen class="size-5" />
  </div>
</PerspectiveBook>
```

## Examples

### Textured

<DemoCanvas>
  <DemoPreview>
    <DemoTextured />
  </DemoPreview>
  <DemoCode>
    ```vue file=./demo-textured.vue
    ```
  </DemoCode>
</DemoCanvas>

## Props

<PropsTable
  data={[
    {
      name: "default",
      type: "VNodeChild",
      nameDetails: "The content to display inside the book"
    },
    {
      name: "size",
      type: "string",
      default: "default",
      typeDetails: "Size of the book. Options: sm, default, lg"
    },
    {
      name: "class",
      type: "string",
      nameDetails: "CSS classes for styling the book cover. Use Tailwind classes like bg-amber-500, bg-red-600, etc."
    },
    {
      name: "textured",
      type: "boolean",
      default: "false",
      nameDetails: "Adds a realistic book texture overlay to the cover"
    }
  ]}
/>